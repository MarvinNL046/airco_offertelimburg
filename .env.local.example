# Dev only — copy to .env.local and fill in the LeadFlow API key.
# Marketplace API key (per-site, scoped on airco niche).
LEADFLOW_API_KEY=lmk_replace_me
LEADFLOW_BASE_URL=https://wetryleadflow.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
