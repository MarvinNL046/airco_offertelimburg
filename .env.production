# Production env — committed, no secrets.
# LEADFLOW_API_KEY + LEADFLOW_BASE_URL worden in Vercel dashboard
# gezet (Server-only env vars). Zie docs/vind-base-template DEPLOYMENT.md.
NEXT_PUBLIC_SITE_URL=https://aircooffertelimburg.nl
